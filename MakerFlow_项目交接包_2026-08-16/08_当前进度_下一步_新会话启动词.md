# 08｜当前进度、尚未完成任务、新会话第一步

## 1. 当前完成状态

### 已完成/主体完成
- MakerFlow项目定位多轮收敛；
- xTool / AImake / Illustrator / Cricut / Atomm工作流研究；
- 2×2机会图；
- 3名受访者访谈；
- JTBD / AS-IS / Problem Priority / MVP范围主体；
- 低保真Demo v1 → v2多轮评审；
- 七步体验主流程；
- Agent Task Graph学习与主要架构；
- Node I/O Matrix；
- Skill Candidate Mapping；
- Skill Registry v1.0草案；
- `svg.render.contract.md`；
- 第一类Contract Eval Cases由用户亲自完成；
- AI PRD Skeleton已让Codex开始做目录/映射/缺失/冲突分析。

### 当前文件中可能仍是旧状态
- `06_node_io_matrix.md`仍保留Apply Safe Fix等旧项；
- `07_Skill_Registry_v1.0.md`仍含`issue.safe_fix`，尚未同步最新决策；
- `svg.render.contract.md`仍把Artifact更新/Preflight invalidation放在Renderer side effect，最新建议移到Orchestrator；
- 旧Task编号可能因删除Safe Fix发生变化，应统一一次；
- Eval机器化资产尚未看到最终文件；
- PRD v0.1正文尚未冻结。

---

# 2. 最新待同步的架构决策

必须同步进文档/Schema：

### CR03
Brief两层状态：
- Field status
- Brief lifecycle + brief_revision

### CR06/07
双Revision：
- design_spec_revision
- artifact_revision
- artifact引用source_design_spec_revision
- preflight绑定checked_artifact_revision

### CR08
外部SVG：
- `authoring_source`
- `design_spec_status`
- external artifact不能假装完整恢复Spec

### CR15/16
Output requirement：
- required / preferred / optional
- required PDF未实现 → partial SVG export + handoff incomplete
- 只有Human可通过新Brief revision取消要求

### CR23/24
Eval生命周期三维：
- definition_status
- execution_status
- latest_run_status

---

# 3. 尚未完成的主要任务

## P0 近期
1. 同步Agent Task Graph / Node I/O / Skill Registry到最新CR决策。
2. 生成Skill Registry v1.1（11个Core Skill）。
3. 修`svg.render.contract`职责。
4. 补核心Contract：
   - brief.extract
   - design_spec.build
   - issue.route
5. Contract Eval Audit。
6. `cases.json + runner + baseline`。
7. Design Spec Schema v0.1。
8. Data / State / Context / Memory Architecture v0.1。
9. Human-AI Responsibility Draft。
10. Ask-vs-Act Policy。
11. AI PRD v0.1第一稿。
12. PRD Cross-document Review。

## P1
- DeepSeek API接入；
- Prompt Inventory与版本管理；
- Semantic Eval；
- Workflow Eval；
- 用户理解测试；
- Demo公开部署；
- Demo视觉优化。

---

# 4. 新会话第一步建议

不要直接开始新的功能。

第一步应做：

> **“状态收口 / Canonical Sync”**

因为当前最新人工决策已经超前于旧文件。

新会话先让助手检查：
- Node I/O
- Task Graph
- Registry
- svg.render Contract
- Eval Case
- PRD Skeleton
是否和最新CR03/06/07/08/15/16/23/24一致。

---

# 5. 可直接复制到新会话的启动Prompt

```text
你现在接手我的MakerFlow AI项目。

请先完整阅读我上传的《MakerFlow项目交接包》，以最新确认决策为最高优先级。

不要重新从零设计产品，也不要根据旧文件把已删除的能力加回来。

当前阶段目标：
1. 先做Canonical Sync：
   - Task Graph
   - Node I/O Matrix
   - Skill Registry
   - Skill Contracts
   - Eval
   - AI PRD
   同步最新CR决策。
2. 然后继续推进：
   Contract Eval可执行化
   → Design Spec / State / Memory
   → Human-AI Responsibility
   → Ask-vs-Act
   → AI PRD v0.1
   → DeepSeek + Prompt Eval。

最新硬决策：
- Safe Fix不属于当前MVP/Core Skill。
- Core Skill收敛为11个。
- confirmed Brief是Field Status + Brief Lifecycle双层状态。
- Design Spec revision与Artifact revision分开。
- External SVG路径中Artifact可以是source of truth，Design Spec可能只是derived partial。
- PDF required但当前只支持SVG时允许partial export，但handoff incomplete，不可偷偷改Brief。
- Eval定义状态、执行状态、最近运行状态必须拆开。
- Model当前仍为Mock，DeepSeek尚未真正接入。
- 不虚构xTool原生集成、生产结果、效率提升。
- PRD与Eval是当前最高学习重心。

回答方式固定使用：
概念是什么
→ MakerFlow例子
→ 我需要懂到什么程度
→ 我必须亲自判断什么
→ 我具体怎么做
→ Codex可以替我做什么及直接可复制Prompt
→ 验收标准

如果发现旧文件与交接包冲突：
只列Conflict，不自行覆盖我的最新人工决定。
```

---

# 6. 当前最合理的一周主线

```text
Canonical Sync
↓
Contract Eval可执行化
↓
Design Spec / State / Context
↓
Human-AI + Ask-vs-Act
↓
AI PRD v0.1
↓
DeepSeek Provider
↓
Prompt v0.1
↓
Contract/Semantic Eval
↓
Workflow Eval
↓
UX测试
↓
PRD v0.2 / Demo v3
```